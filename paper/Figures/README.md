# Paper Assets

This directory gathers the figures and scripts used for publication-quality outputs.

- `figure2.py`: GPLA reproduction of the transient coupling models (formerly under `visualizations/figure2` in PyGPLA_dev).
- `figure2_python.png`: Rendered version using the new `pygpla` API.
