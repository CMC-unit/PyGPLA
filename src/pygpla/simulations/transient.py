"""Transient coupling simulators (placeholder)."""

from __future__ import annotations

import numpy as np

__all__ = ["generate_transient_locked_dataset"]


def generate_transient_locked_dataset(config: dict) -> dict:
    """Generate transiently locked spike/LFP dataset."""

    raise NotImplementedError("Transient simulator not ported yet.")
