"""Phase-locked spike train simulators (placeholder)."""

from __future__ import annotations

import numpy as np

__all__ = ["generate_phase_locked_dataset"]


def generate_phase_locked_dataset(config: dict) -> dict:
    """Generate a synthetic dataset with phase-locked spikes and LFPs."""

    raise NotImplementedError("Phase-locked simulator not ported yet.")
