# Installation

Placeholder for installation instructions.

- Supported Python: 3.10+  
- Recommended: create a virtual environment (conda/venv)
- Install core package: `pip install pygpla` (when published) or `pip install -e .`
- Extras: `pip install -e ".[sim,tests,docs]"`

Add platform-specific notes and troubleshooting here.
